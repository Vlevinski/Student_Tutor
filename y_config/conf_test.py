from y_config import *
import configparser

path = "conf.ini"
config = configparser.ConfigParser()

# create demo config
#cfg = set_string()
#config.read_string(cfg)

# write_conf(config, path)

#config.read(path)

show_config(config)
